const letters = ['a', 'b', 'c'];
//for
for (let index = 0; index < letters.length; index++){
    const element = letters[index]
    console.log(element)
}

const letter = ['a', 'b', 'c', 'd'];

letter.forEach(item => {
    console.log(item)
});